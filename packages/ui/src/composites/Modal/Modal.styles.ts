export const modalSizes = {
  sm: 'max-w-sm',
  md: 'max-w-md',
  lg: 'max-w-lg',
  xl: 'max-w-xl',
  full: 'max-w-full w-full h-full rounded-none',
};

export const modalBackdropClasses = 'fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-[2px] p-4';
export const modalContentClasses = 'bg-surface-secondary border border-border relative w-full rounded-2xl p-6 shadow-2xl focus:outline-none cursor-default';
export const modalCloseButtonClasses = 'absolute right-4 top-4 text-text-primary w-10 h-10 flex items-center justify-center rounded-full hover:bg-surface-card hover:border hover:border-border transition-all duration-fast';
export const modalCloseIconClasses = 'h-5 w-5';
