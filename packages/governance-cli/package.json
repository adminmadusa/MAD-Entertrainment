{
  "name": "@esparex/governance-cli",
  "version": "0.1.0-alpha",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json && chmod +x dist/index.js",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.2"
  }
}
