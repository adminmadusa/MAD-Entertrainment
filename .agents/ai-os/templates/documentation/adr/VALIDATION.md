# ADR Validation

* **Validation Rules**:
  - File must start with a valid YAML header.
  - Headings structure must contain Context and Problem Statement, Decision Outcome, and Consequences.
  - Links must resolve using absolute `file://` scheme.
