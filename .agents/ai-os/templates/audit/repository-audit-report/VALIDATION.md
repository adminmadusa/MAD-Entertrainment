# Repository Audit Report Validation

* **Validation Rules**:
  - File must start with a valid YAML header.
  - Headings must follow specified markdown structure.
  - Links must resolve using absolute `file://` scheme.
