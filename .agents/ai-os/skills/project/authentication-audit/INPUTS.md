# Authentication Audit Inputs

* **Auth Router and Controller source files**: Files located under `apps/server/src/controllers/auth/` and `apps/server/src/routes/auth.routes.ts`.
* **Auth validations schemas**: Validations inside `packages/validations/src/auth.ts`.
