# Naming Audit Inputs

* **Workspace Paths list**: Array of absolute strings representing files and directories inside target workspaces.
* **Scan target**: Glob pattern filter settings (e.g. `**/*`).
