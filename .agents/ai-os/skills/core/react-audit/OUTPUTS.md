# React Safety Audit Outputs

* **Hydration safety warnings**: List of unguarded global parameters accesses that run during component mounting checks.
* **JSX markup errors**: List of dangerous elements.
